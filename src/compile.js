/**
 * Compiles an array into a React DOM structure recursively. All segments of the node array are optional.
 *
 * @param {*[]} node Comprised of a string selector name, params object, string content and child node(s).
 * @return {Object} A react DOM tree built from your arrays recursively.
 */
function compile(node) {
}

var selectorExpression = /(?=\.)(?=#)/g;

module.exports = compile;
